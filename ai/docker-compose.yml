services:

## Minio # --------------------------------------------------------------------------------------------------------------
#  minio:
#    image: pgsty/minio:latest
#    container_name: minio
#    environment:
#      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
#      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
#    volumes:
#      - ./minio:/data
#    command: server /data --console-address ":9001"
#    restart: unless-stopped
#    mem_limit: 1g
#    cpus: "1.0"
#    networks:
#      - proxy_public
#      - ai_internal
#    labels:
#      - "traefik.enable=true"
#      - "traefik.docker.network=proxy_public"
#      - "traefik.http.routers.minio-router.rule=Host(`minio.local`)"
#      - "traefik.http.routers.minio-router.entrypoints=web"
#      - "traefik.http.services.minio-service.loadbalancer.server.port=9001"
#
## n8n # --------------------------------------------------------------------------------------------------------------
#
#  n8n:
#    image: n8nio/n8n:latest
#    container_name: n8n
#    environment:
#      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
#      - N8N_HOST=${SUBDOMAIN}.${DOMAIN_NAME}
#      - N8N_PORT=5678
#      - N8N_PROTOCOL=https
#      - NODE_ENV=production
#      - WEBHOOK_URL=https://${SUBDOMAIN}.${DOMAIN_NAME}/
#      - GENERIC_TIMEZONE=${GENERIC_TIMEZONE}
#      - TZ=${GENERIC_TIMEZONE}
#      - N8N_TRUST_PROXY=true
#      - N8N_PROXY_HOPS=2
#      - DB_TYPE=postgresdb
#      - DB_POSTGRESDB_HOST=postgres
#      - DB_POSTGRESDB_PORT=5432
#      - DB_POSTGRESDB_DATABASE=n8n
#      - DB_POSTGRESDB_USER=n8n
#      - DB_POSTGRESDB_PASSWORD=${DB_N8N_PASSWORD}
#      - N8N_RUNNERS_MODE=external
#      - N8N_RUNNERS_BROKER_LISTEN_ADDRESS=0.0.0.0
#      - N8N_RUNNERS_AUTH_TOKEN=${N8N_RUNNERS_AUTH_TOKEN}
#      - N8N_NATIVE_PYTHON_RUNNER=true
#      - N8N_LICENSE_ACTIVATION_KEY=${N8N_LICENSE_KEY}
#    volumes:
#      - ./n8n:/home/node/.n8n
#      - ./n8n-local:/files
#    restart: unless-stopped
#    mem_limit: 2g
#    cpus: "2.0"
#    labels:
#      - "traefik.enable=true"
#      - "traefik.docker.network=proxy_public"
#      - "traefik.http.routers.n8n-router.rule=Host(`n8n.example.com`)"
#      - "traefik.http.routers.n8n-router.entrypoints=web"
#      - "traefik.http.services.n8n-service.loadbalancer.server.port=5678"
#    networks:
#      - proxy_public
#      - ai_internal
#      - db_internal
#
#  task-runners:
#    image: n8nio/runners:latest
#    container_name: n8n-runners
#    depends_on:
#      - n8n
#    environment:
#      - N8N_RUNNERS_TASK_BROKER_URI=http://n8n:5679
#      - N8N_RUNNERS_AUTH_TOKEN=${N8N_RUNNERS_AUTH_TOKEN}
#      - N8N_RUNNERS_AUTO_SHUTDOWN_TIMEOUT=15
#    restart: unless-stopped
#    mem_limit: 1g
#    cpus: "2.0"
#    networks:
#      - ai_internal
#      - db_internal
#
#networks:
#  proxy_public:
#    external: true
#  ai_internal:
#    external: true
#  db_internal:
#    external: true